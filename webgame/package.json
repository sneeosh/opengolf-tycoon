{
	"name": "opengolf-tycoon-web",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"predev": "node tools/copy-data.mjs",
		"dev": "vite",
		"prebuild": "node tools/copy-data.mjs",
		"build": "tsc --noEmit && vite build",
		"preview": "vite preview",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"pixi.js": "^8.6.6"
	},
	"devDependencies": {
		"typescript": "^5.7.3",
		"vite": "^6.0.11",
		"vitest": "^3.0.5"
	}
}
